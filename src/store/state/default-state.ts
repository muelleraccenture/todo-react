import {AppState} from "./AppState";

export const DEFAULT_STATE: AppState = {
    items: [
        {label: "join experimental noise band", complete: false},
        {label: "become bilingual", complete: true},
    ]
}
